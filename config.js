// ============================================================
// config.js — 環境設定
// ------------------------------------------------------------
// 這支檔案只放「會變動、跟您的部署環境有關」的設定，方便您以後
// 自己調整，不需要動 app.js 的程式邏輯。
// ============================================================

const APP_CONFIG = {
  // 部署好 Cloudflare Worker 之後，把 Worker 網址貼在這裡（結尾不要加斜線）。
  // 範例："https://elvis-gm-api.your-subdomain.workers.dev"
  //
  // 留空字串 "" 的話，總經理會用內建的展示回覆（前端模擬，不會呼叫任何
  // API），網站仍然可以正常展示，只是總經理不是真的在「想」。
  GM_API_ENDPOINT: 'https://elvis-gm-api.elvis-liu2027.workers.dev',

  // V38 新增：所有「寫入」動作（核准／退回、任務推進、成本上限、上傳交付檔案、
  // 新任角色）現在都需要這組密鑰才能通過 Worker 的身分驗證。
  //
  // 設定方法：
  //   1. 在 Cloudflare Worker 專案裡執行 `wrangler secret put ADMIN_KEY`，
  //      或到 Cloudflare Dashboard →「您的 Worker」→ Settings → Variables，
  //      新增一個叫 ADMIN_KEY 的加密環境變數，自己取一組夠長、夠亂的密鑰
  //      （例如用密碼產生器產生 32 個字元的亂數字串）。
  //   2. 把同一組密鑰貼在下面這一行的引號裡。
  //   3. 這支 config.js 是「前端」檔案，部署上網站後任何人都看得到原始碼——
  //      這組密鑰只能防止「不知道網址的人」或「隨手亂試的人」寫入，防不了
  //      「認真檢視過您網站原始碼的人」，請不要用來保護真正機密或高風險的資料。
  //
  // 留空字串 "" 的話：如果 Worker 那邊也沒設定 ADMIN_KEY，維持 V37 以前
  // 「不驗證」的行為；如果 Worker 那邊有設定但這裡沒填，所有寫入動作都會
  // 收到「身分驗證失敗」的錯誤。
  ADMIN_KEY: 'ELVIS_2026_ADMIN_f93kP8qLm72xN4zBvQ91AeHyR6',
};
